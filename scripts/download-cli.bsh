#!/bin/bash
set -e
RELEASE_VERSION=v0.18.0
mkdir -p bin
curl -L https://github.com/operator-framework/operator-sdk/releases/download/${RELEASE_VERSION}/operator-sdk-${RELEASE_VERSION}-x86_64-linux-gnu -o bin/operator-sdk
chmod +x bin/operator-sdk
